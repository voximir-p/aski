[package]
name = "aski"
version = "1.0.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
image = "0.25.10"
terminal_size = "0.4.3"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
